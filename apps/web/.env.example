# Backend API base URL (no trailing slash)
# Example (local): http://localhost:8082 (company service), but prefer a gateway if you have one.
H1B_API_BASE_URL=http://localhost:8082

# Site URL used for absolute URLs in sitemap/metadata (production)
NEXT_PUBLIC_SITE_URL=https://h1bfriend.com
